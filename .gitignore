# Node
node_modules/

# Build
main.js
*.map

# Local scratch
.tmp/

# 封面素材：项目根 assets/cover/ 为用户自有封面图（不再依赖参考项目），随仓库上传；
# demo/assets/cover/ 由脚本从根目录复制而来，是根 assets/cover/ 的副本——不入库（避免重复，本地生成即可）；
# test-local/ 与 obsidian-webnovel-assistant/ 各自整体忽略，其中的 assets 自然不入库。
demo/assets/

# Obsidian
.obsidian/
data.json

# 插件网络封面缓存（运行期产物，不入库）
.gn-data/

# 本地参考项目(不入库)
obsidian-webnovel-assistant/

# 本地实时调试场(随用随覆盖，不入库)
test-local/

# 后续迭代方案记录稿(草案，不入库)
PLAN.md

# AI 交接上下文(工作文档，不入库)
context.md

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Other
.workbuddy
